[
'Template signature
if $TemplateSignature.Valid
	$Prefix = "(Of "
	$Postfix = ""
	foreach $TemplateParameter in $TemplateSignature.ownedParameter
		write $Prefix 
		call TemplateParameter( $TemplateParameter )
		$Prefix = ", "
		$Postfix = ")" 
	next

	write $Postfix
endif
]